# ============================================
# 로컬 개발 환경 설정
# ============================================

# Database Configuration (Local Development)
# 로컬 PostgreSQL 사용 시
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mold_management
DB_USER=postgres
DB_PASSWORD=password

# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=24h

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ============================================
# Railway 프로덕션 환경 설정 예시
# ============================================
# Railway에서 다음 환경 변수를 설정하세요:
#
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# NODE_ENV=production
# PORT=5000
# CLIENT_URL=https://your-frontend.vercel.app
# JWT_SECRET=your-production-secret-key-change-this-to-random-string
# JWT_EXPIRES_IN=24h
# UPLOAD_DIR=./uploads
# MAX_FILE_SIZE=10485760
#
# 참고: Railway는 DATABASE_URL을 자동으로 제공합니다.
# PostgreSQL 서비스를 연결하면 ${{Postgres.DATABASE_URL}} 형식으로 사용 가능합니다.
